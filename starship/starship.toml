"$schema" = 'https://starship.rs/config-schema.json'

format = """
[┌─ ](lavender)\
${custom.ssh_info}\
$directory\
$git_branch\
$git_status\
$fill\
$python\
$lua\
$nodejs\
$golang\
$haskell\
$rust\
$ruby\
$aws\
$docker_context\
$jobs\
$cmd_duration\
$line_break\
[│](lavender)\
$line_break\
[└─](lavender)\
$character"""

add_newline = true
palette = "catppuccin_macchiato"

[character]
# Note the use of Catppuccin color 'peach'
success_symbol = "[[󰄛](green) ❯](peach)"
error_symbol = "[[󰄛](red) ❯](peach)"
vimcmd_symbol = "[󰄛 ❮](subtext1)" # For use with zsh-vi-mode

[battery]
format = "[$symbol$percentage]($style) "
[[battery.display]]
threshold = 50
style = "bold green"
[[battery.display]]
threshold = 100
style = "white"

[directory]
style = 'bold lavender'
format = '[$path ]($style)'
truncation_length = 4
truncation_symbol = '~/'
truncate_to_repo =true 

[git_branch]
style = 'bold mauve'
symbol = ' '
format = '[on](white) [$symbol$branch ]($style)'

[git_status]
style = 'fg:red'
format = '([$all_status$ahead_behind]($style))'

[fill]
symbol = ' '

[python]
style = 'teal'
symbol = ' '
format = '[${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'
pyenv_version_name = true
pyenv_prefix = ''

[lua]
style = 'blue'
format = '[$symbol($version )]($style)'
symbol = ' '

[nodejs]
style = 'bold green'
symbol = ' '

[golang]
style = 'blue'
symbol = ' '

[haskell]
style = 'blue'
symbol = ' '

[rust]
style = 'orange'
symbol = ' '

[ruby]
style = 'blue'
symbol = ' '

[package]
symbol = '󰏗 '

[aws]
symbol = ' '
style = 'yellow'
format = '[$symbol($profile )(\[$duration\] )]($style)'

[docker_context]
symbol = ' '
style = 'fg:#06969A'
format = '[$symbol]($style) $path'
detect_files = ['docker-compose.yml', 'docker-compose.yaml', 'Dockerfile']
detect_extensions = ['Dockerfile']

[jobs]
symbol = ' '
style = 'red'
number_threshold = 1
format = '[$symbol]($style)'

[cmd_duration]
min_time = 500
style = 'fg:custom_foreground'
format = '[$duration]($style)'

# SSH indicator — shows only when connected over SSH
[custom.ssh_info]
command = "whoami | awk '{print toupper(substr($1,1,1)) substr($1,2)}'"
when = '[ -n "$SSH_CONNECTION" ]'
format = '[ssh → $output ]($style)'
style = 'bold cyan'

# Custom Palette
[palettes.catppuccin_macchiato]
rosewater = "#f4dbd6"
flamingo = "#f0c6c6"
pink = "#f5bde6"
mauve = "#c6a0f6"
red = "#ed8796"
maroon = "#ee99a0"
peach = "#f5a97f"
yellow = "#eed49f"
green = "#a6da95"
teal = "#8bd5ca"
sky = "#91d7e3"
sapphire = "#7dc4e4"
blue = "#8aadf4"
lavender = "#b0b8ff"
text = "#cad3f5"
subtext1 = "#b8c0e0"
subtext0 = "#a5adcb"
overlay2 = "#939ab7"
overlay1 = "#8087a2"
overlay0 = "#6e738d"
surface2 = "#5b6078"
surface1 = "#494d64"
surface0 = "#363a4f"
base = "#24273a"
mantle = "#1e2030"
crust = "#181926"

# [palettes.custom_palette]
# custom_dark_blue = '#214969'      # Matches the first color from the ansi array
# custom_blue = '#0FC5ED'           # From the ansi array
# custom_red = '#E52E2E'            # From the ansi array
# custom_green = '#44FFB1'           # From the ansi array
# custom_teal = '#24EAF7'            # Custom teal color
# custom_orange = '#BE5046'         # You can map the cursor color or adjust
# custom_yellow = '#FFE073'         # From the ansi array
# custom_purple = '#A277FF'         # From the brights array
# custom_gray = '#828997'           # Custom gray
# custom_foreground = '#CBE0F0'     # Foreground color from config
# custom_background = '#011423'     # Background color from config
# custom_cursor = '#47FF9C'         # Custom cursor color

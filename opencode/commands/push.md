---
description: Push current branch to remote
agent: build
---

Run `git rev-parse --abbrev-ref HEAD` to get the current branch, then `git push -u origin <branch>` (setting upstream if needed). Report the result.

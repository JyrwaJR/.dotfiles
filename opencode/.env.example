# Environment Variable Template
# Copy to .env and fill in your real values. NEVER commit .env.

# [SECRET] Google Stitch MCP API key
# Used by opencode.jsonc -> mcp.stitch.headers.X-Goog-Api-Key via {env:STITCH_API_KEY}
# Rotate if ever exposed. Get a fresh key from the Google Cloud Console.
STITCH_API_KEY=
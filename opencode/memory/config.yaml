name: dotfiles
description: Agent memory configuration
version: 0.1.0
memory:
  provider: local
  store: ~/.config/memories/local.db
  stores:
    global: ~/.config/memories/local.db
    project: .agent/memory/local.db
